# Copy to jenkins/.env and fill in. `docker compose -f jenkins/compose.yaml`
# reads it automatically (not the repo-root .env); values exported in the
# shell win over this file.

# Frontend API base URL, passed to the Jenkins container and baked into the
# frontend bundle by the Jenkinsfile. Must be the host the Ingress serves.
API_URL=http://saiddit.localhost
